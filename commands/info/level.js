const { Discord, MessageEmbed } = require("discord.js");

module.exports = {
    name: "rank",
    category: "info",
    descripption: "Your level",
    run: async (client, message, args) => {

        
    }
}